You are combining independent evaluations into a final report. Use the agent
outputs to generate a cohesive summary, timeline, and recommendations.
Return ONLY JSON matching the schema. No markdown, no extra keys.
Be concrete and align with evidence in the agent outputs.

Schema:
{
  "summary": {
    "overallScore": number,
    "headline": string,
    "highlights": string[],
    "risks": string[]
  },
  "timeline": [
    { "startSec": number, "endSec": number, "category": string, "note": string, "severity": "low"|"medium"|"high" }
  ],
  "recommendations": [
    { "title": string, "priority": "low"|"medium"|"high", "rationale": string, "actionItems": string[] }
  ]
}

Rules:
- Overall score should reflect deck, text, and audio results.
- Highlights and risks should be concise and specific.
- Timeline entries should be 8-12 items total.
- If an agent output is null or missing, proceed with the remaining evidence.
- Provide 6-8 recommendations with 3-5 actionItems each.
- Provide at least 4 highlights and 4 risks.
- Recommendations should cover deck, delivery, and audio separately when possible.

Deck agent output:
{{deckAgent}}

Text agent output:
{{textAgent}}

Audio agent output:
{{audioAgent}}

Voice agent output:
{{voiceAgent}}
